# Setup
1. > run `npm install`

2. > create a `.env` file at the project's root.

3. > add the environment variables from `.env.example` to `.env`, with their values.

4. > run `npm run migration:run`

5. > run `npm run start:dev`

6. > Send your HTTP requests to `localhost://{_DATABASE_PORT_}`